var generate = function () {

}
//window alert to select length - 8 < length < 128 characters

// confirm if needs lowercase (y or n)

// confirm if needs upppercase (y or n)

// confirm if needs numeric (y or n)

// confirm if needs special characters (y or n)
